// Assign a value to a variable representing a number
var number = 1615;

// Check if the number is even or odd
var isEven = number % 2 === 0;

// Print the result
if (isEven) {
    console.log(number + " is even.");
} else {
    console.log(number + " is odd.");
}
