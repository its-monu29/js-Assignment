var spendMoney = 2000; 
var hasMembershipCard = true; 

if (spendMoney >= 2000 && hasMembershipCard) {
    console.log("The person is eligible to Discount.");
} else {
    console.log("The person is not eligible to discount.");
}
