var isSpecialOffer = true; 
var cravingPizza = false; 

if (isSpecialOffer || cravingPizza) {
    if (isSpecialOffer) {
        console.log("Let's go to our favorite restaurant with a discount!");
    } else {
        console.log("Let's order some pizza!");
    }
} else {
    console.log("Let's cook dinner at home.");
}
