// Given variables
var num1 = 123;
var num2 = 456;

// Extracting the last digit of each number
var lastDigit1 = num1 % 10;
var lastDigit2 = num2 % 10;

// Checking if the last digits are the same
if (lastDigit1 === lastDigit2) {
    console.log("The last digits are the same.");
} else {
    console.log("The last digits are different.");
}

